const numbers =[9, 3, 5, 6, 7, 2];
console.log(numbers);
numbers.sort(); //es para acomodarlos del menor a mayor
console.log(numbers);

const newArray = numbers.sort(); //se crea un nuevo const para el mismo arreglo pero ya ordenado
console.log(newArray);